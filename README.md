# MisApuntes
```
 chupetin
```
## ga

```
ln -s ~/Workspace/notes/dotfiles/.vimrc ~/.vimrc
ln -s ~/Workspace/notes/dotfiles/.vim ~/.vim
rm ~/.zshrc
ln -s ~/Workspace/notes/dotfiles/.zshrc ~/.zshrc
rm ~/.bash_aliases
ln -s Workspace/notes/dotfiles/.bash_aliases ~/.bash_aliases
```
